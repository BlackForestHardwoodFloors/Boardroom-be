import { Router } from "express";

const router = Router();

// Placeholder routes - add your email contact endpoints here later
// router.get("/get-emails/:contactId", getContactEmails);
// router.post("/create-email", createContactEmail);
// router.put("/update-email/:id", updateContactEmail);
// router.delete("/delete-email/:id", deleteContactEmail);

export default router;
